{
  "name": "lambda_layer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf lambda.zip && zip -r -X lambda.zip ./",
    "start:testServer": "node server.js",
    "dev:testServer": "nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.1.0",
    "graphql": "^15.0.0",
    "graphql-request": "^1.8.2",
    "graphql-tag": "^2.10.3",
    "jsonwebtoken": "^8.5.1"
  },
  "devDependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.3"
  }
}
