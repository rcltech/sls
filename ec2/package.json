{
  "name": "ec2",
  "version": "1.0.0",
  "description": "ec2 server for sls data storage",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "connect": "ssh -i .keys/sls-ec2-server.pem ec2-user@ec2-3-17-13-87.us-east-2.compute.amazonaws.com",
    "test": "echo \"No tests specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1"
  }
}
